using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class timeManager : MonoBehaviour {

	// Use this for initialization
	[SerializeField]
	private rotatingLight rl;

	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//Debug.Log((int)Time.time);
	}
}
